generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model TestLog {
  id        Int      @id @default(autoincrement())
  val       Float
  createdAt DateTime @default(now())
}